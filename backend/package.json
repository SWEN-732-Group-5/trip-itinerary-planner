{
	"name": "backend",
	"version": "1.0.0",
	"description": "",
	"main": "src/main.py",
	"scripts": {
		"prepare": "uv sync",
		"uv": "uv",
		"dev:uv": "uv run dev",
		"dev:db": "./db.sh",
		"main": "uv run main",
		"db": "./db.sh",
		"db:status": "docker compose ps",
		"db:up": "docker compose -f docker-compose.yml up",
		"db:down": "docker compose -f docker-compose.yml down",
		"db:delete": "docker compose -f docker-compose.yml down -v",
		"test": "uv run -m pytest --cov --cov-branch --cov-report=xml"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"packageManager": "pnpm@10.33.0",
	"devDependencies": {
		"concurrently": "^9.2.1"
	}
}
